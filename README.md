 /*-------------------------------------------------------------------   Sp Title    : Retrive the hosiptal Details   Author     :    Date Created   :    Description   : Retrive the hosiptal Details   -------------------------------------------------------------   Modification History   ------------------------------------------------------------   Sr No  Date  Modified By   Reason   1.   25-04-2025  Ramesh Arjampudi    SP3V-5007 Changed the received date formate   2.   30-07-2025 Ramesh Arjampudi Added Pharmacy,Clinic and Diagnosis name and Address   3.   04-12-2025 Ramesh Arjampudi SP3V-5323 Clinic name/Address/Pincode/Location/State/District to be incorporated for HCB for all sub service types   -------------------------------------------------------------------*/   --exec Usp_ClaimHospitalizationDetails 25073005305,1  CREATE PROCEDURE [dbo].[Usp_ClaimHospitalizationDetails]  (   @ClaimID bigint,@Slno tinyint  )    AS    BEGIN       SET NOCOUNT ON   /* 1082 - Discharche Typ Leena*/   Declare @ClaimDetailsId BigInt   SELECT  @ClaimDetailsId=Id From ClaimsDetails with(nolock) Where ClaimId=@ClaimId and SlNo=@SlNo      /* End 1082 - Discharche Typ */   /* SP3V-1697*/    Declare @DischargeTypeId Int     Set @DischargeTypeId=0    Set @DischargeTypeId=Isnull((Select top 1 DischargeTypeId From ClaimAdditionalDetails with(nolock) Where ClaimDetailsId=@ClaimDetailsId And Status=1),0)     if @DischargeTypeId = 0      Begin       /*       Select @DischargeTypeId = DischargeTypeId From ClaimAdditionalDetails A with(nolock)        Where DischargeTypeId>0 And ClaimDetailsId in (Select Top 1 Id From ClaimsDetails with(nolock) Where ClaimId=@ClaimId And A.ClaimDetailsId=ID )       */       SELECT @DischargeTypeId = DischargeTypeId        FROM ClaimAdditionalDetails A WITH(NOLOCK)           INNER JOIN ClaimsDetails CD WITH (NOLOCK) ON CD.ID = A.ClaimDetailsId        WHERE DischargeTypeId > 0 AND CD.ClaimId = @ClaimId         Set @DischargeTypeId = IsNull(@DischargeTypeId,0)           End   --End SP3V-1697       select C.ID Claimid,P.ID As ProviderID,(CASE WHEN CAD.PharmacyName IS NOT NULL AND CAD.PharmacyName!='' THEN CAD.PharmacyName        WHEN CAD.ClinicName IS NOT NULL AND CAD.ClinicName!='' THEN CAD.ClinicName        WHEN CAD.DiagnosisName IS NOT NULL AND CAD.DiagnosisName!='' THEN CAD.DiagnosisName ELSE P.Name END) HospitalName,         (CASE WHEN CAD.PharmacyName IS NOT NULL AND CAD.PharmacyName!='' THEN CAD.PharmacyAddress        WHEN CAD.ClinicName IS NOT NULL AND CAD.ClinicName!='' THEN CAD.ClinicAddress        WHEN CAD.DiagnosisName IS NOT NULL AND CAD.DiagnosisName!='' THEN CAD.DiagnosisAddress ELSE '' END) HospitalAddress        --SP3V-1532   ,P.location,CY.Name HospCity,CD.ReqFacilityID,CD.ReqOtherAccm,CD.ApprovedFacilityID,C.ProbableDOA,C.DateofAdmission,C.DateofDischarge,CD.ClaimTypeID,c.ServiceTypeID,C.ServiceSubTypeID,C.HospitalizationType,      CD.RequestTypeID,CD.EstimatedDays,C.RoomDays,C.ICUDays,CD.EstimatedCost,Cd.Claimamount ClaimedAmount,CD.DurationOfAilment,C.FirstConsultation,C.AdmissionTypeID,CD.BedNo,CD.PlanOfTreatment,CD.TypeOfAnesthesiaID,CD.BP,     CD.PR,CD.LMP,CD.Temperature,CD.RS,CD.CVS,CD.Others,CD.ExecutiveNotes,C.PastHistory,C.TreatmentUndertaken,CD.Diagnosis,CD.DrugAdministration,C.PreExistingDisease,CD.InvestigationResults,CD.PresentComplaint,     C.isAccident_RTA,C.DateOfInjury,C.isinjury_alcohol,isAlcoholTested,C.Injury_Occured,C.ReportedToPolice,C.FIRNo,C.FIRDate,C.FIRLocation,C.isMaternity,C.Maternity_G,C.Maternity_P,   C.Maternity_L,C.Maternity_A,Maternity_D,C.ExpdateofDelivery,C.NoOfLivingChildren,CD.icd10code,CD.PCSCode,C.BillNo,C.BillDate,C.IPNo,C.IllnessStartDate,cd.PorA,   case when cd.TreatmentTypeID_P19=2 then 65 when cd.TreatmentTypeID_P19=1 then 66 else cd.TreatmentTypeID_P19 end as TreatmentTypeID_P19,   cd.AilmentType_P18,CD.PaidByPatient,P.PRCNo,P.Address1,P.Address2,C.NatureofTreatmentType_P43,CD.ReceivedDate,C.PatientConditionID,C.PatientConditionDate,CD.InsurerClaimID,mz.Name as ZoneName,Cd.physicalDoc,p.isgipsappn,p.CityID,CD.SkipScrutiny,   (Select top 1  SkipScrutiny from Claimsdetails with(nolock) where ClaimID=@ClaimID And Slno=@Slno-1 And RequestTypeID in(1,2,3)) PreauthSkipScrutiny,CD.IsAutofillby_i3,cd.IsCovid,CD.MemberClaimDocsUploadRefID       --,Isnull((Select top 1  DischargeTypeId From ClaimAdditionalDetails Where ClaimId=@ClaimId And Status=1),0) DischargeTypeId --SP3V1082 Added By Leena   , CRP.PhysicianMobile--added by ganesh   ,CRP.PhysicianName   ,@DischargeTypeId DischargeTypeId --SP3V1082 Added By Leena       ,CAD.PharmacyName,CAD.PharmacyAddress,CAD.ClinicName,CAD.ClinicAddress,CAD.DiagnosisName,CAD.DiagnosisAddress    ,CAD.Pincode as additionalPincode ,CAD.State as additionalState,CAD.District as additionalDistrict, CAD.City as additionalCity,CAD.Location as additionalLocation    ,CAD.HCBProviderName,CAD.HCBProviderAddr   from Claims C with(nolock) Inner Join ClaimsDetails CD with(nolock) on C.id=CD.Claimid    left JOIN ClaimRecPatientDetails CRP with(nolock) ON C.id=CRP.ClaimID--added by ganesh   Inner Join Provider P with(nolock) on P.ID=C.ProviderID and P.Deleted=0   left outer Join mst_City CY with(nolock) on CY.ID=P.CityID     left outer join [Lnk_Zonalpincodes] lzp with(nolock) on  lzp.PinCode = P.PinCode   left outer join Mst_Zones mz with(nolock) on mz.id = lzp.MHDI   LEFT JOIN claimadditionaldetails CAD with(nolock) ON CAD.ClaimDetailsId =  CD.ID  --SP3V-1532      where  CD.Claimid=@ClaimID and CD.Slno=@Slno and C.Deleted=0 and CD.Deleted=0;        select top 1 pr.ProviderstatusID ID,ps.Name from Providercategory Pr with(nolock) inner join Providerstatus PS with(nolock) on PR.providerstatusid=PS.ID,Claims C with(nolock) where Pr.providerid=C.providerID and Pr.deleted=0     and case pr.EntityTypeID_P6 when 159 then c.providerid when 11 then C.issueid  end=pr.RelevantID     and  C.ID=@ClaimID and C.Deleted=0     order by pr.createddatetime;      IF((Select IssueID from claims where ID=@ClaimID)=7)   BEGIN   select claimid,slno,      convert(varchar,(select convert(datetime,(select max(n) from (               values (convert(datetime,(case when Requesttype='Re-settlement' then Convert(datetime,claimcreateddatetime) else Convert(datetime,Claimreceiveddate)end))),      (convert(datetime,CONVERT(datetime,Reopeneddate))),       (convert(datetime,[Date_of_IRretrievalDate])),      (convert(datetime,[ReceiveddatefromInsurer])),      (convert(datetime,[NIDB_RemovedDate])),      (convert(datetime,[Date_of_IR])),      (convert(datetime,[First Reminder])),       (convert(datetime,[Second Reminder])),      (convert(datetime,[RefertoInsurerDate])),      (convert(datetime,[Investigationdate])),      (convert(datetime,[Investigation_RetrievalDate])))      as t(n))))) ldr_date from UVW_ClaimLDRDate1 with(nolock) where claimid=@ClaimID and slno=@Slno   END   ELSE    SELECT @ClaimID claimid,@Slno slno,'' ldr_date     CREATE TABLE #Facility_Claim (ClaimID BIGINT,Slno INT,MOUID BIGINT,PolicyLimit MONEY,PolicyFacility VARCHAR(MAX),TariffFacility INT,TariffCharge MONEY,TariffID BIGINT,ApprovedFacilityID INT,Facility_Rank INT)   INSERT INTO #Facility_Claim EXEC USP_GetApprovedAccomdation @ClaimID,@Slno     SELECT * from #Facility_Claim      END;    
