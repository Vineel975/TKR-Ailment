CREATE Proc [dbo].[USP_GetBlockedAmount]( @MemberPolicyID bigint,@SITypeID tinyint,@claimID bigint=null,@slno int=null)  as  Begin  Declare @statusid int=0,@requesttypeid int  select @statusid=StageID,@requesttypeid=RequestTypeID from CLaimsdetails where ClaimID=@claimID and Slno=@slno and Deleted=0    declare @BPSIIDs varchar(100)='',@MainMemberID bigint,@MemberSIID varchar(100)=''    select @BPSIIDs=@BPSIIDs+convert(varchar,BP.ID)+',',@MemberSIID=@MemberSIID+convert(varchar,MS.ID)+','   from BPSumInsured BP inner join MemberSi MS on BP.id=MS.BPSIID and MS.deleted=0  inner join Mst_PropertyValues mpv on mpv.id=bp.SICategoryID_P20  where BP.Deleted=0 and MS.memberpolicyid=@MemberPolicyID  --select @BPSIIDs;    Select @MainMemberID=MainmemberID from MemberPolicy where ID=@MemberPolicyID and Deleted=0;  if(@SITypeID=5)  set @MemberPolicyID=@MainMemberID;  print @statusid  print @requesttypeid    if(@statusid not in(0,26,27) and @requesttypeid in(7,8,9))  begin  print '1';   with cte(ID,MemberSIID,Categoryname,SumInsured,MemberPolicyID,ClaimID,Slno,ClaimTypeID,RequestTypeID,StageID,SanctionedAmount,ClaimedAmount,BillAmount,SettledAmount   ,isClaimRec,Utilized,BPSIID,ApprovalReq_P60,CalculateCopy_P61)   as   (   Select SICategoryID_P20 ID,MS.ID MemberSIID,MV.Name,BS.SumInsured,MP.ID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount   ,cd.billamount,cd.SettledAmount,   (select top 1 ClaimID from ClaimActionItems where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,   (select sum(SanctionedAmount) from ClaimUtilizedAmount where Claimid=Cd.claimID and Slno=CD.Slno and Deleted=0 and MemberSIID=MS.ID) Utilized,MS.BPSIID,   BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP,Claims C,Claimsdetails CD,MemberSI MS,BPSumInsured BS,Mst_PropertyValues MV   where MP.ID=C.MemberPolicyID and C.ID=CD.ClaimID   and MV.ID=BS.SICategoryID_P20   and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID=69 and Cd.deleted=0   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID   Union   Select SICategoryID_P20,MS.ID MemberSIID,MV.Name,BS.SumInsured+isnull(MS.CB_Amount,0),MP.ID,C.ClaimID,C.Slno,C.ClaimTypeID,c.RequestTypeID,C.StageID,c.SanctionedAmount,c.ClaimedAmount   ,c.billamount,c.SettledAmount,isClaimRec,   C.Utilized,MS.BPSIID,BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP ,MemberSI MS left outer join    (select MemberSIID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount,CD.BillAmount,cd.SettledAmount,MemberPolicyID,   (select top 1 ClaimID from ClaimActionItems where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,CU.SanctionedAmount Utilized      from Claims C Inner join Claimsdetails CD on CD.ClaimID=C.ID and CD.Deleted=0 and C.Deleted=0   Inner join ClaimUtilizedAmount CU on CD.ClaimID=CU.ClaimID and CU.Deleted=0 and CU.Slno=CD.Slno) C on MemberSIID=MS.ID   ,BPSumInsured BS,Mst_PropertyValues MV   where MV.ID=BS.SICategoryID_P20   and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID<>69   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID   )      select BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,sum(BlockedAmt) BlockedAmt,sum(UtilizedAmt) UtilizedAmt,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount    from(   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20 SICategoryID,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0) isAccumulate,BP.isAutoRule,   S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID    where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and isclaimrec is null and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   union   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null   and RequestTypeID in(4,7,8,9)  and stageid in(24,26,27,23,25)   where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   union   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null    and RequestTypeID in(4,7,8,9)  and stageid not in(24,26,27,23,25)   --exists(select 1 from cte where claimid=c.claimid and RequestTypeID in(4,7,8,9) and claimtypeid=1 and stageid not in(24,26,27,23,25) )   where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   ) BA   group by BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount  End  Else  if(@statusid not in(0,26,27))  begin   with cte(ID,MemberSIID,Categoryname,SumInsured,MemberPolicyID,ClaimID,Slno,ClaimTypeID,RequestTypeID,StageID,SanctionedAmount,ClaimedAmount,BillAmount,SettledAmount   ,isClaimRec,Utilized,BPSIID,ApprovalReq_P60,CalculateCopy_P61)   as   (   Select SICategoryID_P20 ID,MS.ID MemberSIID,MV.Name,BS.SumInsured,MP.ID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount   ,cd.billamount,cd.SettledAmount,   (select top 1 ClaimID from Claimsdetails where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,   (select sum(SanctionedAmount) from ClaimUtilizedAmount where Claimid=Cd.claimID and Slno=CD.Slno and Deleted=0 and MemberSIID=MS.ID) Utilized,MS.BPSIID,   BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP,Claims C,Claimsdetails CD,MemberSI MS,BPSumInsured BS,Mst_PropertyValues MV   where MP.ID=C.MemberPolicyID and C.ID=CD.ClaimID   and MV.ID=BS.SICategoryID_P20   and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID=69 and cd.deleted=0   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID   Union   Select SICategoryID_P20 ID,MS.ID MemberSIID,MV.Name,BS.SumInsured,MP.ID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount   ,cd.billamount,cd.SettledAmount,   (select top 1 ClaimID from Claimsdetails where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,   (select sum(SanctionedAmount) from ClaimUtilizedAmount where Claimid=Cd.claimID and Slno=CD.Slno and Deleted=0 and MemberSIID=MS.ID) Utilized,MS.BPSIID,   BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP left outer join Claims C on MP.ID=C.MemberPolicyID and C.deleted=0   left outer join Claimsdetails CD on C.ID=CD.ClaimID   and cd.deleted=0,MemberSI MS,BPSumInsured BS,Mst_PropertyValues MV   where  MV.ID=BS.SICategoryID_P20 and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID<>69   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID    )       select BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,sum(BlockedAmt) BlockedAmt,sum(UtilizedAmt) UtilizedAmt,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount    from(   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20 SICategoryID,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0) isAccumulate,BP.isAutoRule,   S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and @Claimid<>C.ClaimID   where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and isclaimrec is null and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount      --Chaged bY Srinu B on 2nd Nov 2016 --Start--       --union    --select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20 SICategoryID,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)    --,UtilizedAmt=isnull(sum(case when c.stageid=26 then isnull(SanctionedAmount,0)  when c.stageid=27 then isnull(SettledAmount,0) end),0),    --C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0) isAccumulate,BP.isAutoRule, S.CB_Amount    --from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and @Claimid<>C.ClaimID    --where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and isclaimrec is not null and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20    --group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount       union    select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)    ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,    isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount    from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null and @Claimid<>C.ClaimID    and RequestTypeID in(7,8,9)     where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20    group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,    S.CB_Amount      union    select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)    ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,    isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount    from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null and @Claimid<>C.ClaimID    and RequestTypeID in(4) and ClaimTypeID=2    where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20    group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,    S.CB_Amount      union    select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)    ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,    isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount    from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null and @Claimid<>C.ClaimID    and RequestTypeID in(4) and ClaimTypeID=1 and    exists(select 1 from cte where claimid=c.claimid  and claimtypeid=1 and stageid not in(24,26,27,23,25) )    where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20    group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,    S.CB_Amount    --Chaged bY Srinu B on 2nd Nov 2016 --END--   ) BA   group by BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount  End  Else  begin  print 'else';   with cte(ID,MemberSIID,Categoryname,SumInsured,MemberPolicyID,ClaimID,Slno,ClaimTypeID,RequestTypeID,StageID,SanctionedAmount,ClaimedAmount,BillAmount,SettledAmount   ,isClaimRec,Utilized,BPSIID,ApprovalReq_P60,CalculateCopy_P61)   as   (   Select SICategoryID_P20 ID,MS.ID MemberSIID,MV.Name,BS.SumInsured,MP.ID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount   ,cd.billamount,cd.SettledAmount,   (select top 1 ClaimID from ClaimActionItems where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,   (select sum(SanctionedAmount) from ClaimUtilizedAmount where Claimid=Cd.claimID and Slno=CD.Slno and Deleted=0 and MemberSIID=MS.ID) Utilized,MS.BPSIID,   BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP,Claims C,Claimsdetails CD,MemberSI MS,BPSumInsured BS,Mst_PropertyValues MV   where MP.ID=C.MemberPolicyID and C.ID=CD.ClaimID   and MV.ID=BS.SICategoryID_P20   and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID=69 and Cd.deleted=0   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID   Union   Select SICategoryID_P20,MS.ID MemberSIID,MV.Name,BS.SumInsured+isnull(MS.CB_Amount,0),MP.ID,C.ClaimID,C.Slno,C.ClaimTypeID,c.RequestTypeID,C.StageID,c.SanctionedAmount,c.ClaimedAmount   ,c.billamount,c.SettledAmount,isClaimRec,   C.Utilized,MS.BPSIID,BS.ApprovalReq_P60,BS.CalculateCopy_P61   from MemberPolicy MP ,MemberSI MS left outer join    (select MemberSIID,CD.ClaimID,CD.Slno,CD.ClaimTypeID,cd.RequestTypeID,CD.StageID,cd.SanctionedAmount,c.ClaimedAmount,CD.BillAmount,cd.SettledAmount,MemberPolicyID,   (select top 1 ClaimID from ClaimActionItems where Claimid=Cd.claimID and RequestTypeID=4 and claimtypeid=CD.ClaimTypeID) isClaimRec,CU.SanctionedAmount Utilized      from Claims C Inner join Claimsdetails CD on CD.ClaimID=C.ID and CD.Deleted=0 and C.Deleted=0   Inner join ClaimUtilizedAmount CU on CD.ClaimID=CU.ClaimID and CU.Deleted=0 and CU.Slno=CD.Slno) C on MemberSIID=MS.ID   ,BPSumInsured BS,Mst_PropertyValues MV   where MV.ID=BS.SICategoryID_P20   and MS.memberpolicyid=MP.ID and MS.Deleted=0 and MS.BPSIID=BS.ID and MS.BPSIID in(select StringValue from fn_Split(@BPSIIDs,','))   and MV.ID<>69   and case when @SITypeID=5 then MP.MainmemberID  when @SITypeID=6 then MP.ID end=@MemberPolicyID   )      select BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,sum(BlockedAmt) BlockedAmt,sum(UtilizedAmt) UtilizedAmt,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount    from(   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20 SICategoryID,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0) isAccumulate,BP.isAutoRule,   S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID    where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and isclaimrec is null and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   union   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null   and RequestTypeID in(4,7,8,9)   where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   union   select S.BPSIID,S.ID MemberSIID,bp.SICategoryID_P20,MV.name Categoryname,bp.SumInsured,BlockedAmt=isnull(sum(case when stageid in(24,29) then Utilized end),0)   ,UtilizedAmt=isnull(sum(case when stageid=26 then SanctionedAmount  when stageid=27 then SettledAmount end),0),C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   from bpsuminsured bp,Mst_PropertyValues MV,MemberSI S left outer join cte C on C.BPSIID=S.BPSIID and isclaimrec is not null and   exists(select 1 from cte where claimid=c.claimid and RequestTypeID in(4,7,8,9) and claimtypeid=1 and stageid not in(24,26,27,23,25) )   where S.ID in(select StringValue from fn_Split(@MemberSIID,',')) and bp.id=s.BPSIID and MV.ID=Bp.SICategoryID_P20   group by S.BPSIID,bp.SICategoryID_P20,MV.name ,bp.SumInsured,S.ID,C.ApprovalReq_P60,C.CalculateCopy_P61,isnull(BP.isAccumulate,0),BP.isAutoRule,S.CB_Amount   ) BA   group by BPSIID,MemberSIID,SICategoryID,Categoryname,SumInsured,ApprovalReq_P60,CalculateCopy_P61,isAccumulate,isAutoRule,CB_Amount  End  End  
